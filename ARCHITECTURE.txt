╔══════════════════════════════════════════════════════════════════════════════╗
║                        tstorage 时序存储引擎架构                              ║
╚══════════════════════════════════════════════════════════════════════════════╝


┌─────────────────────────────────────────────────────────────────────────────┐
│                              用户接口层                                       │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  Storage 接口                                                               │
│  ├── InsertRows(rows []Row) error           ──→  写入数据                   │
│  ├── Select(metric, labels, start, end)     ──→  查询数据                   │
│  └── Close() error                          ──→  关闭存储                   │
│                                                                             │
│  数据结构：                                                                  │
│  ├── Row {Metric, Labels, DataPoint}                                        │
│  └── DataPoint {Value, Timestamp}                                           │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                            核心存储层 (storage.go)                            │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  storage 结构体                                                              │
│  ├── partitionList  ──────────────────→  分区链表管理                       │
│  ├── wal           ──────────────────→  预写日志                             │
│  ├── workersLimitCh ────────────────→  并发控制（GOMAXPROCS）              │
│  ├── wg (WaitGroup) ────────────────→  等待写入完成                         │
│  ├── doneCh                       →  关闭信号                               │
│  ├── partitionDuration  ──────────→  分区时长（默认1小时）                  │
│  ├── retention                   →  保留时间（默认14天）                    │
│  ├── timestampPrecision          →  时间戳精度（默认纳秒）                   │
│  ├── dataPath                    →  磁盘存储路径                             │
│  └── writeTimeout                →  写入超时（默认30秒）                     │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                    ┌─────────────────┴─────────────────┐
                    │                                   │
                    ▼                                   ▼
┌───────────────────────────────┐       ┌───────────────────────────────┐
│       分区管理层              │       │         WAL 层                 │
├───────────────────────────────┤       ├───────────────────────────────┤
│                               │       │                               │
│  partitionList (链表)         │       │  wal 接口                     │
│  ├── head ──→ 最新分区        │       │  ├── append(op, rows)        │
│  ├── tail ──→ 最旧分区        │       │  ├── flush()                 │
│  ├── insert() ──→ O(1)        │       │  ├── punctuate()             │
│  ├── remove() ──→ O(n)        │       │  └── removeOldest()          │
│  └── swap()   ──→ O(n)        │       │                               │
│                               │       │  实现：                        │
│  分区类型：                    │       │  ├── nopWAL (内存模式)       │
│  ├── MemoryPartition (可写)   │       │  └── diskWAL (磁盘模式)      │
│  └── DiskPartition (只读)     │       │                               │
│                               │       │  WAL 段文件：                 │
│  partition 接口：              │       │  ├── 000001.wal              │
│  ├── insertRows()             │       │  ├── 000002.wal              │
│  ├── selectDataPoints()       │       │  └── ...                      │
│  ├── minTimestamp()           │       │                               │
│  ├── maxTimestamp()           │       └───────────────────────────────┘
│  ├── size()                   │
│  ├── active()                 │
│  └── expired()                │
└───────────────────────────────┘
                    │
        ┌───────────┴───────────┐
        │                       │
        ▼                       ▼
┌───────────────────────┐ ┌───────────────────────┐
│   MemoryPartition     │ │   DiskPartition       │
├───────────────────────┤ ├───────────────────────┤
│                       │ │                       │
│ 内存分区（可写）       │ │ 磁盘分区（只读）       │
│                       │ │                       │
│ 结构：                 │ │ 结构：                 │
│ ├── metrics (sync.Map) │ │ ├── dirPath           │
│ ├── wal                │ │ ├── meta (元数据)     │
│ ├── duration           │ │ ├── f (文件句柄)      │
│ ├── precision          │ │ ├── mappedFile (mmap) │
│ ├── outOfOrderPoints   │ │ └── retention         │
│ └── outOfOrderCap      │ │                       │
│                       │ │ 磁盘文件：             │
│ metrics 结构：         │ │ ├── p-{min}-{max}/    │
│ ├── name               │ │ │   ├── data (压缩)   │
│ ├── labels             │ │ │   └── meta.json     │
│ ├── points []DataPoint │ │ │                      │
│ ├── minTimestamp       │ │ └── wal/              │
│ ├── maxTimestamp       │ │     ├── 000001.wal    │
│ ├── lastTimestamp      │ │     └── ...           │
│ └── size               │ │                       │
│                       │ │ 特性：                 │
│ 特性：                 │ │ ├── mmap 内存映射      │
│ ├── 有序切片存储       │ │ ├── 数据压缩          │
│ ├── 支持乱序数据       │ │ ├── 按需读取          │
│ ├── 高缓存命中率       │ │ └── 只读访问          │
│ └── CPU 缓存友好       │ │                       │
└───────────────────────┘ └───────────────────────┘
                    │                       │
                    │                       │
                    ▼                       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                            辅助组件层                                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  encoding.go                  ──→  数据编码/解码                            │
│  ├── newSeriesEncoder()                                                     │
│  ├── encodePoint()                                                         │
│  └── decodePoint()                                                         │
│                                                                             │
│  bstream.go                   ──→  位流处理（压缩）                         │
│  ├── writeBit()                                                            │
│  ├── writeBits()                                                           │
│  └── readBits()                                                            │
│                                                                             │
│  label.go                     ──→  标签处理                                │
│  ├── labelsKey()                                                           │
│  └── marshalMetricName()                                                   │
│                                                                             │
│  logger.go                    ──→  日志记录                                │
│  ├── Printf()                                                             │
│  └── Println()                                                            │
│                                                                             │
│  internal/cgroup/             ──→  资源限制                                │
│  ├── AvailableCPUs()                                                      │
│  └── 内存监控                                                             │
│                                                                             │
│  internal/syscall/            ──→  系统调用封装                            │
│  ├── Mmap()                                                                │
│  └── 平台相关实现                                                         │
│                                                                             │
│  internal/timerpool/          ──→  定时器池                                │
│  ├── Get()                                                                 │
│  └── Put()                                                                 │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘


╔══════════════════════════════════════════════════════════════════════════════╗
║                              数据流向图                                       ║
╚══════════════════════════════════════════════════════════════════════════════╝


【写入流程】

用户调用 InsertRows()
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│ 1. 获取 worker 令牌（并发控制）                               │
│    workersLimitCh ← struct{}{}                               │
└─────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│ 2. 确保 head 分区是活动的                                     │
│    ensureActiveHead()                                        │
│    ├── 如果 head 不活动 → 创建新内存分区                     │
│    └── 插入到 partitionList 头部                            │
└─────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│ 3. 写入 WAL（预写日志）                                       │
│    wal.append(operationInsert, rows)                         │
│    ├── 编码数据                                              │
│    ├── 写入缓冲区                                            │
│    └── 缓冲区满时刷新到磁盘                                  │
└─────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│ 4. 插入到分区                                                 │
│    partition.insertRows(rows)                                │
│    ├── 检查时间戳范围                                        │
│    ├── 处理乱序数据（存入 outOfOrderPoints）                │
│    ├── 获取或创建 metric                                     │
│    └── 添加数据点到 points 切片                              │
└─────────────────────────────────────────────────────────────┘
        │
        ▼
    返回过时的行（如果时间戳早于分区范围）


【查询流程】

用户调用 Select(metric, labels, start, end)
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│ 1. 参数验证                                                   │
│    ├── metric 不能为空                                       │
│    └── start < end                                           │
└─────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│ 2. 创建分区迭代器                                             │
│    iterator := partitionList.newIterator()                   │
│    ├── 使用 Dummy Node                                       │
│    └── 从最新分区开始                                        │
└─────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│ 3. 遍历分区                                                   │
│    for iterator.next() {                                     │
│        part := iterator.value()                              │
│        ├── 检查时间范围是否匹配                               │
│        ├── 跳过不相关的分区                                   │
│        └── 从分区查询数据                                    │
│    }                                                          │
└─────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│ 4. 从分区查询数据                                             │
│    part.selectDataPoints(metric, labels, start, end)         │
│    ├── MemoryPartition: 遍历 metrics，过滤数据点             │
│    └── DiskPartition: mmap 读取，解码数据点                  │
└─────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│ 5. 合并并排序结果                                             │
│    ├── 收集所有分区的结果                                     │
│    └── 按时间戳升序排序                                      │
└─────────────────────────────────────────────────────────────┘
        │
        ▼
    返回 []*DataPoint


【持久化流程】

触发条件：创建新分区、Close() 调用
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│ 1. 遍历内存分区（跳过前2个可写分区）                          │
│    for partition in partitionList[2:] {                     │
└─────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│ 2. 压缩数据                                                   │
│    flush(dirPath, memoryPartition)                           │
│    ├── 创建目录 p-{minTimestamp}-{maxTimestamp}             │
│    ├── 创建 data 文件                                        │
│    ├── 编码所有指标的数据点                                  │
│    ├── 刷新缓冲区                                            │
│    └── 创建 meta.json（分区元数据）                          │
└─────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│ 3. 创建磁盘分区                                               │
│    diskPartition := openDiskPartition(dirPath)               │
│    ├── 使用 mmap 映射 data 文件                              │
│    ├── 读取 meta.json                                        │
│    └── 创建 diskPartition 对象                               │
└─────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│ 4. 替换分区                                                   │
│    partitionList.swap(memoryPartition, diskPartition)        │
│    └── 在链表中替换节点                                      │
└─────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│ 5. 清理 WAL                                                   │
│    wal.removeOldest()                                        │
│    └── 删除对应的 WAL 段文件                                 │
└─────────────────────────────────────────────────────────────┘


【WAL 恢复流程】

程序启动
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│ 1. 加载磁盘分区                                               │
│    扫描 data 目录                                            │
│    ├── 打开所有 p-* 目录                                      │
│    ├── mmap 映射 data 文件                                    │
│    └── 读取 meta.json                                        │
└─────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────┐
│ 2. 恢复 WAL 数据                                             │
│    recoverWAL(walDir)                                        │
│    ├── 读取所有 WAL 段文件                                   │
│    ├── 解码 WAL 记录                                         │
│    ├── 调用 InsertRows() 重新插入数据                         │
│    └── 删除 WAL 文件                                         │
└─────────────────────────────────────────────────────────────┘
        │
        ▼
    创建新的可写分区


╔══════════════════════════════════════════════════════════════════════════════╗
║                              目录结构                                           ║
╚══════════════════════════════════════════════════════════════════════════════╝

./data/
├── p-1600000001-1600003600/          ← 磁盘分区 1
│   ├── data                          ← 压缩的数据文件（mmap）
│   └── meta.json                     ← 分区元数据
│       ├── MinTimestamp
│       ├── MaxTimestamp
│       ├── NumDataPoints
│       └── Metrics {                 ← 指标索引
│           "metric1": {
│               Name
│               Offset                ← data 文件中的偏移量
│               MinTimestamp
│               MaxTimestamp
│               NumDataPoints
│           }
│       }
├── p-1600003601-1600007200/          ← 磁盘分区 2
│   ├── data
│   └── meta.json
└── wal/                              ← 预写日志目录
    ├── 000001.wal                    ← WAL 段文件 1
    ├── 000002.wal                    ← WAL 段文件 2
    └── ...


╔══════════════════════════════════════════════════════════════════════════════╗
║                              核心设计原则                                       ║
╚══════════════════════════════════════════════════════════════════════════════╝

1. 分区架构（Partition-based）
   ├─ 按时间范围分区（默认1小时）
   ├─ 每个分区独立存储
   ├─ 支持内存分区和磁盘分区
   └─ 最新数据在内存中，历史数据在磁盘

2. 预写日志（WAL）
   ├─ 数据写入前先记录 WAL
   ├─ 防止数据丢失
   └─ 崩溃后可恢复

3. 并发控制
   ├─ 限制并发写入数量（GOMAXPROCS）
   ├─ 读写锁保护分区列表
   ├─ 原子操作计数
   └─ WaitGroup 等待写入完成

4. 内存映射（mmap）
   ├─ 磁盘分区使用 mmap
   ├─ 按需读取，不全部加载
   ├─ 操作系统管理缓存
   └─ 高效访问历史数据

5. 乱序数据处理
   ├─ 保留2个可写分区
   ├─ 临时缓冲乱序数据
   ├─ 刷新时合并
   └─ 丢弃过期的乱序数据

6. 数据压缩
   ├─ 使用位流编码
   ├─ Gorilla 压缩算法
   ├─ 按指标单独压缩
   └─ 减少存储空间


╔══════════════════════════════════════════════════════════════════════════════╗
║                              性能特征                                           ║
╚══════════════════════════════════════════════════════════════════════════════╝

写入性能：
├─ 批量插入优化
├─ 并发写入控制
├─ WAL 缓冲区减少磁盘 I/O
└─ 有序切片提供高缓存命中率

查询性能：
├─ 分区过滤减少扫描范围
├─ mmap 零拷贝读取
├─ 索引快速定位指标
└─ 按需解码数据点

内存占用：
├─ 只有元数据在堆中
├─ 磁盘分区使用 mmap
├─ 乱序数据容量限制
└─ 可配置保留时间

磁盘 I/O：
├─ 批量刷新减少写入次数
├─ 顺序写入优化
├─ 数据压缩减少存储
└─ WAL 刷盘可配置


╔══════════════════════════════════════════════════════════════════════════════╗
║                              配置选项                                           ║
╚══════════════════════════════════════════════════════════════════════════════╝

WithDataPath(path)           ← 指定磁盘存储路径
WithPartitionDuration(d)     ← 设置分区时长（默认1小时）
WithRetention(d)             ← 设置数据保留时间（默认14天）
WithTimestampPrecision(p)    ← 设置时间戳精度（默认纳秒）
WithWriteTimeout(t)          ← 设置写入超时（默认30秒）
WithLogger(logger)           ← 设置日志记录器
WithWALBufferedSize(size)    ← 设置 WAL 缓冲区大小（默认4096）


╔══════════════════════════════════════════════════════════════════════════════╗
║                              适用场景                                           ║
╚══════════════════════════════════════════════════════════════════════════════╝

✓ 实时监控系统
✓ 指标采集和分析
✓ IoT 设备数据存储
✓ 应用性能监控（APM）
✓ 日志分析
✓ 负载测试工具
✓ 时序数据可视化

不适用：
✗ 需要复杂查询的场景
✗ 需要事务支持的场景
✗ 需要关系型数据的场景
✗ 需要水平扩展的场景（单机）


═══════════════════════════════════════════════════════════════════════════════
                                    架构图结束
═══════════════════════════════════════════════════════════════════════════════